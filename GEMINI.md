# Gemini CLI adapter

@./AGENTS.md

`AGENTS.md` is the canonical Champagne repository law. Gemini must also load `ROUTER_REPOSITORY_POLICY_V1.json`, the Stage 1 read-first canon named by `AGENTS.md`, and the nearest scoped `AGENTS.md` under `apps/**` or `packages/**`.

No stage inherits authority from another stage. Stop on missing imports, canon conflict, semantic uncertainty, Hero/sacred-surface scope expansion, PHI/clinical expansion, chatbot-engine or Router mutation, merge, deploy, publication, or production authority without an exact separate gate.

This adapter grants no additional authority.
