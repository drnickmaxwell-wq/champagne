# Stock Canon

These specifications are authoritative and canon locked.
All stock-domain implementations must conform to these documents.
The stock domain is non-PHI; do not include PHI.
The tenant identity model is required: use `tenantId` (never `practiceId`).
