# Content Scope

This folder defines the publish scope for the build.

- `live-pages.json`: Ordered list of public routes that ship for launch.
- `archive-pages.json`: Reserved list for archived routes (currently empty).
- `delete-pages.json`: Reserved list for routes slated for removal (currently empty).
