import { RegistryBrowser } from "./_components/RegistryBrowser";
import { Room11Composer } from "./_components/Room11Composer";
import { FLOWS } from "./data/flows";

export default function DesignLabPage() {
  return <main className="dl-main"><section className="dl-intro"><p className="dl-kicker">FOUNDER VISUAL DECISION STUDIO</p><h1>The Champagne Design Lab</h1><p>Explore all 331 recovered V27 designs, build an ordered shortlist, compare six semantic page compositions and export a durable Founder brief. This is the complete visual library—not a catalogue of grey placeholders.</p><div className="dl-intro-stats"><span><strong>331</strong> visual designs</span><span><strong>12</strong> evidence rooms</span><span><strong>6</strong> exemplar flows</span><span><strong>0</strong> production bindings</span></div></section><section className="dl-flow-section"><div className="dl-section-heading"><div><p className="dl-kicker">PAGE COMPOSITION STUDIES</p><h2>Six semantic exemplars</h2></div><p>Use these to compare pacing and section ownership. The visual archive below supplies the actual design language.</p></div><div className="dl-flow-grid">{FLOWS.map((flow) => <a key={flow.id} href={`/champagne/design-lab/exemplars/${flow.slug}`}><strong>{flow.family === "home" ? "Homepage" : flow.family === "implants" ? "Dental Implants" : "Composite Bonding"} · {flow.variant}</strong><span>{flow.sections.length} semantic jobs · Composition {flow.variant}</span><small>{flow.id}</small></a>)}</div></section><RegistryBrowser /><Room11Composer /></main>;
}
