# Scoped AGENTS.md — Champagne Hero package

This file narrows the root Champagne `AGENTS.md` for `packages/champagne-hero/**`.

## Sacred Hero law

- Treat Hero registry, config, manifest adaptation, runtime, surface mapping, rendering contracts, motion, fallbacks, and asset resolution as sacred/high-risk.
- Diagnostic, preview, content, SEO, page, styling, or integration authority does not imply Hero authority.
- Do not modify Sacred Hero paths unless the exact task explicitly grants the required Hero surgeon authority and names the files.
- Preserve manifest → adapter → runtime → renderer alignment, canonical asset identity, surface semantics, reduced-motion behaviour, deterministic fallback, and failure visibility.
- Do not force visibility, hard-code assets/styles, bypass gating, replace a failure with a decorative gradient, or make visual guesses without diagnostics.
- Do not rename canonical identifiers, registry keys, manifest fields, motion states, or surface names.
- No raw colours or styling outside approved token/semantic contracts.
- Do not modify website, manifests, guards, Router, chatbot engine, or another repository from Hero scope unless separately and exactly authorised.
- No merge, deploy, publication, production, or credential authority.

## Required approach

1. inspect data/manifest truth;
2. confirm asset resolution;
3. inspect runtime and layer gating;
4. identify the exact broken seam;
5. make the smallest repair;
6. run focused Hero tests plus `pnpm run guard:hero`, relevant canon guards, and required build verification.

If authority or the rendering cause is uncertain, stop and report diagnostics rather than patching blindly.
