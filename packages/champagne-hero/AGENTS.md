# Scoped Instructions — Champagne Hero Package

**Status:** `ACTIVE_SCOPED_CONSTRAINTS_NO_AUTHORITY`

Applies to `packages/champagne-hero/**`. It narrows the root instructions and
grants no authority.

- Treat registry, config, manifest adaptation, runtime, surface mapping,
  rendering contracts, motion, fallbacks and asset resolution as sacred/high-risk.
- Diagnostic, preview, content, SEO, page, styling or integration scope does not
  imply Hero mutation authority.
- Sacred Hero mutation requires fresh exact Founder authority naming the files
  and Hero scope.
- Preserve manifest → adapter → runtime → renderer alignment, canonical asset
  identity, surface semantics, reduced-motion behaviour, deterministic fallback
  and failure visibility.
- Never force visibility, hard-code assets/styles, bypass gating, substitute a
  decorative gradient for a failure or guess without diagnostics.
- Do not rename canonical identifiers, registry keys, manifest fields, motion
  states or surface names.
- Do not expand into website, manifests, guards or another repository without
  separate exact authority.
- Required method: inspect truth, resolve assets, inspect gating, identify the
  seam, make the smallest authorised repair, run focused Hero tests and the
  applicable full proving ladder.
- Stop when authority or root cause is uncertain.
