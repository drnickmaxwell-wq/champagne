# Runtime Page Coverage Report

## Runtime registry
- **ChampagnePageBuilder** resolves a manifest for each slug via `getPageManifestBySlug` or direct machine manifest lookups, then loads hero presets, CTA slots, and the section stack for rendering.【F:apps/web/app/(champagne)/_builder/ChampagnePageBuilder.tsx†L18-L195】
- Section stacks come from `getSectionStack`, which normalizes manifest or layout entries for the renderer.【F:packages/champagne-sections/src/ChampagneSectionRenderer.tsx†L33-L123】

## Routes
- **/** → `apps/web/app/page.tsx`; renders `HeroRenderer` (if enabled) plus `ChampagnePageBuilder` with slug `/` (manifest-driven). Sections render in manifest order: `home_intro_positioning` (copy-block), `home_authority` (feature-grid), `home_treatment_hub` (feature-list), `home_care_pathway` (steps), `home_treatment_routing` (routing_cards), `home_special_pathways` (routing_cards), `home_technology_showcase` (media), `home_patient_stories` (patient_stories_rail), `home_google_reviews` (google_reviews), `home_finance_options` (feature-list), `home_faq` (treatment_faq_block), `home_closing_cta` (treatment_closing_cta).【F:apps/web/app/page.tsx†L1-L13】【F:packages/champagne-manifests/data/champagne_machine_manifest_full.json†L7-L242】
- **/treatments** → `apps/web/app/treatments/page.tsx`; hard-coded index that groups `getTreatmentPages()` output from the manifest, with hero optionally enabled, and links to each treatment path. No section stack is used here.【F:apps/web/app/treatments/page.tsx†L1-L147】
- **/treatments/[slug]** → `apps/web/app/treatments/[slug]/page.tsx`; dynamic route that resolves a treatment manifest via `getTreatmentManifest`, derives `pageSlug`, and feeds it into `ChampagnePageBuilder` (manifest/layout-driven sections). NotFound is returned when the manifest is missing.【F:apps/web/app/treatments/[slug]/page.tsx†L1-L85】 Manifest sections for treatments (e.g., implants) reference layout-driven IDs like `treatment.heroIntro`, `treatment.trustSignals`, etc., which are expanded by section layouts into concrete components.【F:packages/champagne-manifests/data/champagne_machine_manifest_full.json†L244-L280】
- **/contact** → `apps/web/app/contact/page.tsx`; uses `ChampagnePageBuilder` with slug `/contact` pulling manifest sections: `contact_intro_copy` (copy-block), `contact_details_simple` (feature-list), and `contact_followup_cta` (cta). The `cta` type is not registered and falls back to a text block in the renderer.【F:apps/web/app/contact/page.tsx†L1-L14】【F:packages/champagne-manifests/data/champagne_machine_manifest_full.json†L486-L530】【F:packages/champagne-sections/src/ChampagneSectionRenderer.tsx†L60-L70】
- **/patient-portal** → `apps/web/app/patient-portal/page.tsx`; renders a static `IntentLanding` explainer plus `ChampagnePageBuilder` for `/patient-portal`. Manifest sections include `patient_portal_intro_copy` (copy-block), `patient_portal_features` (feature-list), and `patient_portal_cta_band` (cta → falls back to text).【F:apps/web/app/patient-portal/page.tsx†L1-L115】【F:packages/champagne-manifests/data/champagne_machine_manifest_full.json†L578-L620】【F:packages/champagne-sections/src/ChampagneSectionRenderer.tsx†L60-L70】
- **/practice-plan** → `apps/web/app/(site)/[page]/page.tsx`; the catch-all normalizes the slug to `/practice-plan`, verifies a manifest exists, then passes it to `ChampagnePageBuilder`. Manifest sections render in order: `practice_plan_intro` (copy-block), `practice_plan_included` (feature-list), `practice_plan_joining` (steps), `practice_plan_fit` (feature-list), `practice_plan_cta` (cta → fallback).【F:apps/web/app/(site)/[page]/page.tsx†L1-L33】【F:packages/champagne-manifests/data/champagne_machine_manifest_full.json†L778-L849】【F:packages/champagne-sections/src/ChampagneSectionRenderer.tsx†L60-L70】
- **/video-consultation** → `apps/web/app/(site)/[page]/page.tsx`; resolves manifest `/video-consultation` to render sections: `video_consultation_intro` (copy-block), `video_consultation_scope` (feature-list), `video_consultation_limitations` (feature-list), `video_consultation_flow` (steps), `video_consultation_suitability` (feature-list), `video_consultation_cta` (cta → fallback).【F:apps/web/app/(site)/[page]/page.tsx†L1-L33】【F:packages/champagne-manifests/data/champagne_machine_manifest_full.json†L622-L704】【F:packages/champagne-sections/src/ChampagneSectionRenderer.tsx†L60-L70】
- **/finance** → `apps/web/app/(site)/[page]/page.tsx`; resolves manifest `/finance` and renders sections: `finance_intro` (copy-block), `finance_options` (feature-list), `finance_discussion_flow` (steps), `finance_expectations` (feature-list), `finance_cta` (cta → fallback).【F:apps/web/app/(site)/[page]/page.tsx†L1-L33】【F:packages/champagne-manifests/data/champagne_machine_manifest_full.json†L706-L776】【F:packages/champagne-sections/src/ChampagneSectionRenderer.tsx†L60-L70】

## Missing or downgraded sections
- Section type `cta` is present on `/contact`, `/patient-portal`, `/practice-plan`, `/video-consultation`, and `/finance`, but no dedicated component is registered; the renderer falls back to `Section_TextBlock`, so CTA-specific layouts do not render.【F:packages/champagne-manifests/data/champagne_machine_manifest_full.json†L486-L776】【F:packages/champagne-sections/src/ChampagneSectionRenderer.tsx†L60-L70】
